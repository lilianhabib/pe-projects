{
	"manifest_version": 3, 
	"version": "1.1", 
	"name": "Leads tracker", 
	"action": { //what happens when users click the app from the google chrome extension tool bar
		"default_popup": "index.html", // this is the popup - which is the html that we have been working on. 
		"default_icon": "icon.png" //pop up beside the puzzle piece 
	}, 

	"permissions": [
		"tabs"
	]
}